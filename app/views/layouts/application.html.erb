<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <% if current_user %>
        <meta name="current_user" content="<%= current_user.id %>">
    <% end %>
    <link rel="shortcut icon" type="image/png" href="http://jsoncodes.com/wp-content/uploads/2016/03/Favicon.png"/>
    <title><%= content_for?(:title) ? yield(:title) : "Make" %></title>
    <script src="//maps.google.com/maps/api/js?sensor=true"></script>
		<%= stylesheet_link_tag "application" %>
		<%= javascript_include_tag "application", 'data-turbolinks-track' => true %>

		<%= csrf_meta_tags %>
  </head>
  <body>
   YourBrowserSucks.com
  </body>
</html>
